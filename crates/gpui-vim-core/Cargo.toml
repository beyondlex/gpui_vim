[package]
name = "gpui-vim-core"
description = "A host-agnostic Vim engine: modes, motions, operators, text objects, registers, search and marks."
version = "0.1.2"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
regex = { workspace = true }
unicode-width = { workspace = true }

[dev-dependencies]
# ropey-backed buffer for the probe's "good host" cases (the naive B above
# demonstrates the bad-host cost; see PERFORMANCE.md §1)
ropey = { workspace = true }
